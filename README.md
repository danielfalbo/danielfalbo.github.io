# danielfalbo.github.io

:octocat:

## Commands

macOS:

```bash
# Sort `quotes.js`
make

# Find duplicates
make duplicates
```

## More Commands

macOS:

```bash
# Install prettier
make prettier

# Export `quotes.json`
make json

# Sort `quotes.js` (and keep `quotes.json`)
make sort_js

# Sort `quotes.json`
make sort_json

# Delete `qutoes.json`
make clean
```
